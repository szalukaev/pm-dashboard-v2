# ═══════════════════════════════════════════════════════
# PM Dashboard V3 — Environment
# DB credentials, Redmine API key — настраиваются через Setup Wizard
# и хранятся в SQLite (/data/config.db)
# ═══════════════════════════════════════════════════════

APP_PORT=8080
DATA_DIR=/data
REDIS_URL=redis://redis:6379/0
